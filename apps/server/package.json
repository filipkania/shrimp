{
  "name": "@shrimp/server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "cargo run",
    "build": "cargo build --release",
    "test": "cargo test",
    "gen:bindings": "cargo test --lib"
  }
}
